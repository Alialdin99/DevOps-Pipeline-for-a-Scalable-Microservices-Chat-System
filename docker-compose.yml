version: "3.10"

services:
  web: 
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes: 
      - /app/node_modules
      - .:/app
    command: ["npm", "run", "start"]

  app: 
    build:
      context: .
      dockerfile: Dockerfile
    volumes: 
      - /app/node_modules
      - .:/app
    command: ["npm", "run", "start"]
